def find_max(numbers):
    if len(numbers) == 0:
        return None

    max_value = numbers[0]
    for num in numbers:
        if num > max_value:
            max_value = num

    return max_value

# 示例用法
numbers = [5, 9, 3, 1, 7]
max_value = find_max(numbers)
print("最大值:", max_value)
